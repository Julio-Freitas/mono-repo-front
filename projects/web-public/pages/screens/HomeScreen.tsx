import { sum } from "@alura/utils/macth";
import { Text } from "@alura/ds";

export function HomeScreen() {
  return (
    <div>
      <Text tag="h1">Home do Projetos</Text>
      <Text tag="p">@alura/utils/math/sum: sum(2,2) = {sum(2, 2)}</Text>
    </div>
  );
}
